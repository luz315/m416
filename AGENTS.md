# 작업 지침

이 저장소는 Java 21 / Spring Boot 기반 Stay Gateway다. 현재 제품 구조는 `docs/architecture.md`를 따른다.

## 필요한 문서만 읽기

- 목적·범위: [docs/project.md](docs/project.md)
- 구조·의존성 변경: [docs/architecture.md](docs/architecture.md)
- 환경·명령·작성 관례: [docs/development.md](docs/development.md)
- Java 모듈 규칙: [docs/architecture.md](docs/architecture.md) — 기술 독립적인 Core와 외부 어댑터 경계를 우선한다.
- Java 스타일: [docs/java-style.md](docs/java-style.md) — Java 구현·리뷰 때만 읽기
- 비즈니스 동작 변경: [docs/domain/overview.md](docs/domain/overview.md)
- 모델 배정·위임 판단: [docs/model-policy.md](docs/model-policy.md)
- 검증·완료 판단: [docs/verification.md](docs/verification.md)
- 중요한 설계 결정: [docs/decisions/README.md](docs/decisions/README.md)
- 장기 작업 기록: [docs/tasks/TEMPLATE.md](docs/tasks/TEMPLATE.md)

## 대화 언어와 적용 승인

- 채팅의 설명, 진행 안내, 질문, 선택지, 승인 요청은 한국어로 작성한다. 직접 작성하는 선택지도 ‘예/아니요’처럼 한국어로 표시한다. 코드 식별자·명령은 정확한 원문을 유지하며, 앱이 표시하는 버튼과 도구 원문은 번역을 보장할 수 없다. 이 규칙은 파일 내부 언어를 강제하지 않는다.
- 매 작업의 변경 전에 계획, 변경 대상, 영향 범위, 검증 명령을 제시하고 추가할 사항과 누락 가능성을 검토한다. 사용자에게 추가·수정할 내용이 있는지와 적용 승인을 묻고, 승인 전에는 적용하지 않는다. 작은 수정에도 이 절차를 적용한다.
- 계획에 필요한 파일 조회·검색·분석은 바로 수행할 수 있다. 파일 생성·수정·삭제와 검증·빌드 등의 실행은 계획에 포함해 승인받은 뒤 수행한다. 계획을 채팅에 제시하는 데 파일 생성은 필요하지 않다.
- 사용자가 제시한 계획에 ‘진행해’, ‘오케이’ 등으로 승인하면 그 범위의 수정과 검증을 묶어서 수행한다. 이미 승인된 작업을 명령마다 다시 묻지 않는다. 변경 범위가 확대되거나 계획의 중요한 전제가 바뀌면 새 계획을 설명하고 다시 승인받는다.

## 작업 방식


- 기존 코드와 사용자 변경을 확인하고 요청 범위에 필요한 변경을 수행한다.
- Main Agent가 작업을 이어서 수행한다. Skill은 반복 절차이며 별도의 에이전트가 아니다.
- 작은 수정도 짧은 계획과 승인을 거쳐 처리한다. 기능 개발 전체 흐름에는 `build-feature`, 계획만 요청하면 `plan-feature`, 구현에는 `implement-feature`, 리뷰에는 `review-code`, 검증에는 `verify-feature`를 사용한다. 위치는 `.agents/skills/<이름>/SKILL.md`다.
- 문서의 미정 항목을 사실로 가정하지 않는다. 결과를 바꾸는 핵심 요구사항만 질문하고, 독립적인 조회·분석과 이미 승인된 범위의 작업은 계속한다. 합리적인 가정은 결과에 명시한다.
- 여러 단계·세션에 걸치는 작업만 `docs/tasks/`에 목표, 결정, 진행 상황, 다음 작업을 기록한다. 단순 수정에는 작업 문서를 요구하지 않는다.
- 모델 배정은 `docs/model-policy.md`를 따른다. 계획에서 승인받은 범위의 독립 반복 작업은 `light-worker`, 복잡한 분석·검토는 `deep-worker`에 자동 위임한다. worker는 한 번에 하나만 생성하고 재위임하지 않는다. 일반 작업과 작은 수정은 Main이 직접 처리한다.
- 같은 실패에 진전 없이 재시도하지 않는다. 원인을 좁히고 외부 조건이나 사용자 결정이 필요하면 근거와 재개 조건을 보고한다.

## 사용량 절약

- 필요한 문서·Skill·코드만 읽고 관련 부분부터 검색한다. 작은 작업에 모든 Skill을 적용하지 않는다.
- 계획과 결과는 작업 크기에 맞게 짧게 쓴다. 긴 로그·전체 파일 복사와 중복 탐색·구현을 피한다.
- 새 변경이나 실패 근거가 없으면 통과한 검사를 반복하지 않는다. 환경 오류 때문에 모델을 상향하지 않는다.

## 완료 기준

- 요청한 동작과 인수 조건을 확인하고 변경에 맞는 검증을 실행한다.
- 실행한 명령과 결과를 구분해 보고한다. 미실행·환경 차단·검증 미설정을 통과로 표현하지 않는다.
- 변경으로 틀려진 관련 문서만 함께 갱신한다. 공통 사실은 기준 문서에 한 번만 기록한다.
- 최종 보고에는 변경 내용, 검증 결과, 남은 제약을 담는다. 검증 범위를 넘어 완료를 주장하지 않는다.
